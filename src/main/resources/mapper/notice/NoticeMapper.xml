<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spacecore.mapper.notice.NoticeMapper">

    <resultMap id="NoticeMap" type="com.spacecore.domain.notice.Notice">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="pinned" property="pinned"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findAll" resultMap="NoticeMap">
        SELECT id, user_id, title, content, pinned, created_at
        FROM notices
        ORDER BY CASE pinned WHEN 'Y' THEN 0 ELSE 1 END, created_at DESC
    </select>

    <select id="findById" parameterType="long" resultMap="NoticeMap">
        SELECT id, user_id, title, content, pinned, created_at
        FROM notices
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.spacecore.domain.notice.Notice">
        <selectKey keyProperty="id" resultType="long" order="BEFORE">
            SELECT seq_notices.NEXTVAL FROM dual
        </selectKey>
        INSERT INTO notices (id, user_id, title, content, pinned, created_at)
        VALUES (#{id}, #{userId}, #{title}, #{content}, COALESCE(#{pinned}, 'N'), SYSTIMESTAMP)
    </insert>

    <update id="update" parameterType="com.spacecore.domain.notice.Notice">
        UPDATE notices
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="pinned != null">pinned = #{pinned},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM notices WHERE id = #{id}
    </delete>

    <select id="search" parameterType="string" resultMap="NoticeMap">
        SELECT id, user_id, title, content, pinned, created_at
        FROM notices
        WHERE title LIKE '%' || #{keyword} || '%'
        OR content LIKE '%' || #{keyword} || '%'
        ORDER BY CASE pinned WHEN 'Y' THEN 0 ELSE 1 END, created_at DESC
    </select>

    <select id="findPinned" resultMap="NoticeMap">
        SELECT id, user_id, title, content, pinned, created_at
        FROM notices
        WHERE pinned = 'Y'
        ORDER BY created_at DESC
    </select>

</mapper>