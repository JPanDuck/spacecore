<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spacecore.mapper.auth.RefreshTokenMapper">

    <!-- ✅ 토큰으로 조회 -->
    <select id="findByToken" parameterType="string" resultType="com.spacecore.domain.auth.RefreshToken">
        SELECT id, user_id, token, expiry_date
        FROM refresh_tokens
        WHERE token = #{token}
    </select>

    <!-- ✅ user_id로 조회 -->
    <select id="findByUserId" parameterType="long" resultType="com.spacecore.domain.auth.RefreshToken">
        SELECT id, user_id, TRIM(token) AS token, expiry_date
        FROM refresh_tokens
        WHERE user_id = #{userId}
    </select>

    <!-- ✅ 기본 insert -->
    <insert id="insert" parameterType="com.spacecore.domain.auth.RefreshToken">
        INSERT INTO refresh_tokens (id, user_id, token, expiry_date)
        VALUES (seq_refresh_tokens.NEXTVAL, #{userId}, #{token}, #{expiryDate})
    </insert>

    <!-- ✅ 삭제 -->
    <delete id="deleteByUserId" parameterType="long">
        DELETE FROM refresh_tokens WHERE user_id = #{userId}
    </delete>

    <!-- ✅ upsert (MERGE) -->
    <insert id="saveOrUpdate">
        MERGE INTO refresh_tokens t
            USING (SELECT #{userId} AS user_id FROM dual) s
            ON (t.user_id = s.user_id)
            WHEN MATCHED THEN
                UPDATE SET t.token = #{token}, t.expiry_date = #{expiry}
            WHEN NOT MATCHED THEN
                INSERT (id, user_id, token, expiry_date)
                    VALUES (seq_refresh_tokens.NEXTVAL, #{userId}, #{token}, #{expiry})
    </insert>

</mapper>